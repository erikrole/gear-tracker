UI System: Apple “Liquid Glass” inspired + Wisconsin typography

- Tailwind tokens for:
  colors (neutral base + Wisconsin red accent)
  radii (rounded-2xl default)
  shadows (soft)
  blur (backdrop blur for cards)
- Components:
  GlassCard, Button, Badge, Input, Select, Tabs, Modal/Sheet, Toast, DataList
- Mobile-first layout:
  bottom nav or thumb-friendly primary actions
  large touch targets
  minimal tables by default (toggleable table view ok)
- Typography:
  Aeternus Tall: headers/metrics only
  Gotham: body/UI
  Wisconsin font: decorative logo only