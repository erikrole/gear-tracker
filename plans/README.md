# Implementation Plans

Generated by the improve skill on 2026-06-10. Execute in the order below unless dependencies say otherwise. Each executor must read the selected plan fully before starting, honor its STOP conditions, and update the status row when done.

## Execution Order And Status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make CI prove the production app build without live migration state | P1 | S/M | none | DONE |
| 002 | Bound calendar-source sync fetches | P1 | M | none | DONE |
| 003 | Report calendar sync changes truthfully | P1 | S/M | 002 | DONE |
| 004 | Rehost external BulkSku images through Blob | P2 | M/L | none | DONE |
| 005 | Split iOS profile and availability views out of AppTabView | P2 | M | none | DONE |
| 006 | Refresh the testing guide against the current suite | P3 | S | none | DONE |
| 007 | Make database diagnostics use live migration truth | P2 | M | 001 | DONE |
| 008 | Normalize custom API limit parsing | P3 | S | none | DONE |
| 009 | Register iOS UserAvatarView in the audit inventory | P1 | S | none | DONE |
| 010 | Extend iOS API error guardrails into Core | P1 | M | none | DONE |
| 011 | Consolidate iOS avatar rendering | P2 | S/M | 009 | DONE |
| 012 | Add an iOS TestFlight build metadata gate | P2 | S | none | DONE |
| 013 | Split iOS CreateBookingSheet into focused pieces | P2 | M | 011 | DONE |
| 014 | Kiosk checkout completion validates availability, friendly 409s, first tests | P1 | M | none | DONE |
| 015 | Fix kiosk activation-code lifecycle dead end; make docs truthful | P2 | S | none | DONE |
| 016 | Make kiosk lastSeenAt update durable on serverless (`after()`) | P2 | S | none | DONE |
| 017 | Route iOS kiosk checkout completion through shared error path | P3 | S | none | DONE |
| 018 | Compose app error boundaries with shadcn primitives | P2 | S | none | DONE |
| 019 | Wrap SelectItem lists in SelectGroup | P2 | M | none | DONE |
| 020 | Consolidate status-dot UI on a semantic primitive | P3 | M | none | DONE |
| 021 | Route the sidebar user avatar through the shared UserAvatar wrapper | P2 | S | none | DONE |
| 022 | Make AssetImage the shadcn-backed item thumbnail primitive | P2 | S/M | none | DONE |
| 023 | Consolidate item thumbnail stacks on AvatarGroup and AssetImage | P2 | M | 022 | DONE |
| 024 | Migrate the settings audit log table to shadcn Table | P2 | S | none | DONE |
| 025 | Migrate the sports shift-count matrix to shadcn Table | P2 | S/M | none | DONE |
| 026 | Migrate the schedule assignment grid to shadcn Table | P2 | M | none | DONE |
| 027 | Consolidate dashboard queue rows on shadcn Item | P2 | M | none | DONE |
| 028 | Add missing shadcn Sheet descriptions | P2 | S | none | DONE |
| 029 | Route destructive AlertDialog actions through shadcn variants | P2 | S/M | none | DONE |
| 030 | Render booking details as a mobile Drawer | P2 | M | none | DONE |
| 031 | Centralize firmware watch source and coverage rules | P1 | M | none | TODO |
| 032 | Add a firmware watch settings surface | P1 | M/L | 031 | TODO |
| 033 | Expand firmware watch coverage with verified official sources | P2 | M/L | 031, 032 | TODO |
| 034 | Preserve multi-event links on reservation duplicate and conversion | P1 | S/M | none | TODO |
| 035 | Consolidate booking create and edit audit ownership | P2 | M | 034 | TODO |
| 036 | Guard booking edit windows before availability rewrites | P2 | S | none | DONE |
| 037 | Share dashboard counts with stats transient lanes | P1 | M | none | TODO |
| 038 | Surface dashboard partial-result warnings | P2 | S/M | none | TODO |
| 039 | Make dashboard overflow links filter-aware | P2 | S | none | DONE |
| 040 | Make booking Step 1 event selection explicit | P1 | S/M | none | TODO |
| 041 | Add booking Step 1 field-level validation | P2 | M | none | TODO |
| 042 | Preserve booking Step 1 manual edits after event changes | P2 | M | none | TODO |
| 043 | Make Step 2 available-only use derived status | P1 | S/M | none | TODO |
| 044 | Block Step 2 resolved unavailable selected assets | P1 | M | none | TODO |
| 045 | Remove Step 2 hidden fifty-result ceiling | P2 | M | none | TODO |
| 046 | Show Step 3 item-level availability warnings | P2 | M | none | TODO |
| 047 | Apply Step 3 bulk shortage recovery | P1 | S/M | none | TODO |
| 048 | Gate Step 3 on availability recheck | P2 | S/M | none | TODO |
| 049 | Prevent Quantity add-to-existing from adjusting unit families | P1 | S/M | none | BLOCKED: `BulkSkuOption` omits `trackByNumber`; plan scope must include `src/components/FormCombobox.tsx` |
| 050 | Capture item-family unit, threshold, and department on create | P2 | M | none | TODO |
| 051 | Add item-family image handoff after create | P2 | S/M | none | TODO |
| 052 | Align item-detail actions with booking eligibility | P1 | S/M | none | DONE |
| 053 | Add upcoming-reservation avatars to item detail | P2 | S/M | none | TODO |
| 054 | Make item delete affordance match safety policy | P2 | S | 052 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency Notes

- 003 depends on 002 to avoid two executors editing `src/lib/services/calendar-sync.ts` and calendar-source UI tests at the same time.
- 004 includes a Prisma migration and should be executed alone relative to other schema work.
- 005 is intentionally isolated from feature work in `ios/Wisconsin/Views/AppTabView.swift`; do not combine it with new iOS settings behavior.
- 007 depends on 001 only if the executor wants to use `build:app` as the build gate. It can otherwise run with the existing full `npm run build` in an environment that has `DIRECT_URL`.
- 011 depends on 009 so the shared avatar component is represented in the audit inventory before more avatar work expands its role.
- 013 depends on 011 to avoid overlapping edits in `CreateBookingSheet.swift` and `UserAvatarView`.
- 014-017 come from a Kiosk-focused audit pass (2026-06-10, planned at `e8566c54`). They are independent of each other, but 014 and 015 both add a change-log row to `docs/AREA_KIOSK.md` -- run sequentially or expect a trivial merge there.
- Do not run 017 concurrently with 010: both touch iOS API error handling (017 consumes `APIError` in `KioskAPIClient.swift`; 010 works in `Core`). Sequential execution avoids conflicting assumptions about `APIError` cases.
- Plans 014-017 were written without user selection (non-interactive default: top findings by leverage). Unplanned kiosk findings are recorded below so they are not re-audited.
- Plans 018-020 come from a focused shadcn component-system audit (2026-06-11, planned at `8022c88d`). They are independent, but 018 and 019 both add to `tests/shadcn-component-contracts.test.ts`; run sequentially or merge that test file carefully. Plan 020 can append to the same test after either one lands.
- Plans 021-023 come from a focused shadcn avatar and item-thumbnail audit (2026-06-11, planned at `c90837ce`). Plan 021 is independent. Plan 023 depends on 022 because the stack should consume the shadcn-backed `AssetImage` instead of recreating image fallback behavior.
- Plans 024-027 come from a focused shadcn Table and Item audit (2026-06-11, planned at `c90837ce`). Plans 024-026 all append to `tests/shadcn-table-item-contracts.test.ts`; run sequentially or merge that source-contract file carefully. Plan 026 is intentionally after 024 and 025 in recommended order because the assignment grid has sticky-column behavior and a child component that currently returns its own raw `td`. Plan 027 is independent from the table plans.
- Plans 028-030 come from a focused shadcn Sheet, Drawer, Dialog, and overlay-flow audit (2026-06-11, planned at `c90837ce`). Plans 028-030 all append to `tests/shadcn-overlay-contracts.test.ts`; run sequentially or merge that source-contract file carefully. Plan 030 should run after 028 if an executor wants the overlay-description contract in place before adding the Drawer path, but it is not technically blocked by 028.
- Plans 031-033 come from a focused firmware watch audit (2026-06-11, planned at `8d445512`). Run 031 first so source normalization and support rules are centralized before adding management UI or more coverage. Run 032 before 033 if humans need an admin surface to validate and manage newly discovered targets. Plan 033 requires current official-source checks and must not guess manufacturer URLs.
- Plans 034-036 come from a focused booking-flow audit (2026-06-11, planned at `8d445512`). Plan 034 should run before 035 because conversion and duplicate route tests will clarify which route-level audit rows are true action breadcrumbs. Plan 036 is independent and small. Do not run 035 concurrently with other booking audit-log changes.
- Plans 037-039 come from a focused dashboard audit (2026-06-11, planned at `8d445512`). Plan 037 should run before any new dashboard count consumers because it centralizes count truth. Plan 038 is independent but pairs well with 037 because both touch dashboard payload metadata. Plan 039 is a small UI consistency slice and can run separately.
- Plans 040-042 come from a focused booking Step 1 UX/UI audit (2026-06-11, planned at `8d445512`). Plan 040 is the best first slice because it fixes the visible event-selection control and cap feedback without changing wizard state shape. Plan 041 is independent. Plan 042 touches reducer semantics and should run after any concurrent Step 1 UI edits land.
- Plans 043-045 come from a focused booking Step 2 audit (2026-06-11, planned at `8d445512`). Plan 043 is first because the available-only filter should match derived status before more picker-result behavior is layered on. Plan 044 is independent but adjacent to 043. Plan 045 can run separately and should not expand availability checks to every matching row.
- Plans 046-048 come from a focused booking Step 3 audit (2026-06-11, planned at `8d445512`). Plan 047 is the strongest first slice because it fixes submit-time recovery without changing picker state. Plan 046 and 048 are independent of 047, but they both touch the warning flow between Step 2 and Step 3, so run them sequentially or merge `BookingWizard.tsx`, `WizardStep3.tsx`, and `flow-summary.ts` carefully.
- Plans 049-051 come from a focused Add Item flow audit (2026-06-11, planned at `8d445512`). Plan 049 should run first because it protects unit-tracked item-family data integrity. Plans 050 and 051 both touch `new-item-sheet.tsx`, `BulkItemForm.tsx`, source-contract tests, and Items docs, so run them sequentially or merge those files carefully.
- Plans 052-054 come from a focused item detail page audit (2026-06-11, planned at `8d445512`). Plan 052 should run first because it aligns the visible Reserve and Check out entry points with server booking availability. Plan 054 depends on 052 only because both extend the same item-detail action source-contract test. Plan 053 is independent and can run separately.

## Findings Considered And Rejected

- Add a broad CSRF change to public `withHandler` routes: not planned in this pass. The confirmed CSRF gap in `withAuth` is already fixed, and public auth routes need separate product/security review because they intentionally accept unauthenticated POSTs with rate limits.
- Treat the current npm audit output as a top plan: not planned. `npm audit --audit-level=high --omit=dev` passed, and the remaining moderate advisories are already recorded in `docs/GAPS_AND_RISKS.md`.
- Fix the `assertPublicHost` DNS-rebinding residual now: not planned. The risk is real but already documented as deferred, and a correct fix needs a broader fetch-helper design across image and calendar paths.

From the Kiosk pass (2026-06-10):

- Location-scope kiosk booking mutations (`/api/kiosk/checkout/[id]`, `checkin/[id]/*`, `pickup/[id]/*` accept any booking id from any activated kiosk, and `actorId` is not bound to the booking requester): not planned. D-032 deliberately scoped only the reads; booking ids are unguessable cuids reachable only via the location-scoped student endpoint, and the documented trust model (staffed counter, physical device gate) carries the weight. Needs a maintainer product call before tightening -- revisit if kiosks ever deploy to less-trusted contexts.
- `pickup/[id]/scan` serialized duplicate-scan check-then-act race (findFirst + create outside a transaction): not worth a standalone plan. Worst case is a redundant successful scan event; confirm logic is unaffected. Noted as an opportunistic fix in plan 014's maintenance notes.
- `kiosk-devices` PATCH validates its body by hand instead of Zod (`src/app/api/kiosk-devices/[id]/route.ts`): not planned. The manual `typeof` checks are safe; consistency-only cleanup, fold into the next change that touches that file.
- Enforce one-time activation codes / always-on kiosk sessions: deferred product decision, documented in plan 015's maintenance notes (May 20 commit `94580cc6` made sessions always-on; June 2 reinstated 7-day expiry; the two halves are currently incoherent and plan 015 fixes only the operational dead end).
- Kiosk direction gaps already tracked in `docs/AREA_KIOSK.md` Known Gaps (roster search/first-letter filter for larger rosters; "wrong person" undo inside the kiosk): grounded feature options, not planned here.

From the Firmware Watch pass (2026-06-11):

- Add all skipped camera bodies immediately: rejected. Current docs explicitly require verified official source URLs and parser-backed metadata. Seeding guessed URLs would make daily polling noisy and less trustworthy.
- Remove the `CANON_SUPPORT` enum value: rejected for this pass. There are no live Canon camera bodies in the current report, and removing an enum value is migration work with little user value. Plan 031 instead makes unsupported schema source types fail explicitly.
- Add a manual "poll now" button: deferred. The higher-value admin action is testing a source before saving it. Daily polling remains owned by morning refresh until there is evidence that manual polling is needed operationally.

From the Booking Flow pass (2026-06-11):

- Rebuild the booking wizard again: rejected. Current creation docs, client validation, draft restore, event-load recovery, and picker warning behavior are already well-covered. The higher-leverage findings are backend continuity and audit ownership.
- Add new booking list scopes: rejected. The unified Bookings page already defaults to active checkouts and reservations, with past work behind an explicit scope.
- Rework kiosk pickup security from booking-flow findings: rejected in this pass. Kiosk trust model and known gaps are already documented, and recent plans 014-017 cover the active kiosk hardening work.

From the Dashboard pass (2026-06-11):

- Reorder pending pickups beyond the current `take: 5`: rejected. The query orders by `startsAt ASC`, so late pickups naturally sort ahead of future pickups before the cap is applied.
- Replace dashboard location filter URL values with location ids: rejected for this pass. It is a plausible polish issue if duplicate location names appear, but current filter options and row payloads are name-based throughout the dashboard.
- Rewrite dashboard data normalization to be immutable: deferred. Mutating React Query data during normalization is not ideal, but the current high-signal user bugs are count drift, fallback visibility, and filter-link consistency.

From the Booking Step 1 UX/UI pass (2026-06-11):

- Add range selection for multi-event picking: rejected. The brief marks shift-click range selection as nice-to-have, and the higher-value issue is making the existing selection state clear and explainable.
- Redesign the booking wizard header: rejected. Recent docs and code already moved the title to the selected event or booking name. The current Step 1 issues are interaction clarity and field feedback, not hero layout.
- Add long instructional copy for event auto-fill: rejected. The area docs explicitly keep context copy compact and reserve longer guidance for real errors or review.

From the Booking Step 2 pass (2026-06-11):

- Reintroduce picker tab counts: rejected. The current area docs intentionally keep section tabs as labels and put selected counts in the step header and selected shelf.
- Add a select-visible bulk action: rejected. The product pattern says deliberate per-item selection, search, or scan is the real workflow; broad selection adds clutter and accidental mass-selection risk.
- Treat hard conflicts as unavailable: rejected. Current docs allow conflict-warning rows to remain selectable for staff override. This pass only blocks current derived unavailable assets.

From the Booking Step 3 pass (2026-06-11):

- Redesign the confirmation hero again: rejected. Recent Step 3 work already aligns the review panel with the area docs. The remaining issues are warning specificity, 409 recovery, and stale recheck gating.
- Add longer kiosk pickup instructions to confirmation: rejected. The checkout area doc explicitly keeps the notice concise: kiosk scan starts custody.
- Block every hard-conflict selection before submit: rejected. Current booking policy allows selected conflict-warning rows to continue for staff override while submit-time server validation remains authoritative.

From the Add Item flow pass (2026-06-11):

- Convert Add Item into a full multi-step wizard: rejected. Current docs intentionally keep Add Item as a sheet for one-by-one intake, while large batches should use `/import`.
- Add drafts to Add Item: rejected. `AREA_ITEMS.md` explicitly keeps draft recovery out of V1 Add Item and reserves it for full-page wizards such as booking creation.
- Add Units add-to-existing inside Add Item now: deferred. The stockroom/Battery Ops route already owns audited Add units behavior with unit-number creation and operator reasons. Plan 049 first closes the unsafe Quantity path before any new unit-family intake shortcut is considered.

From the Item Detail pass (2026-06-11):

- Redesign the item detail layout again: rejected. Recent item detail docs show the header, Info grid, QR dialog, Attachments tab, and wide-layout follow-ups have already shipped. The higher-value issues now are action-policy correctness and schedule identity consistency.
- Add a new QR uniqueness plan: rejected. Manual QR entry already PATCHes the asset route, and that route catches Prisma unique conflicts with a 409 for `qr_code_value`; a nicer preflight can be folded into future QR polish.
- Rebuild the schedule tab calendar bars: rejected. The current Schedule tab already has month bars, an agenda list, keyboard-open rows, and booking-sheet drill-in. The confirmed gap is only missing avatar data for upcoming reservations.
