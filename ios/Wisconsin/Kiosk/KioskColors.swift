import SwiftUI

extension Color {
    /// Wisconsin red used by every kiosk surface. Single source so brand
    /// updates touch one file instead of five.
    static let kioskRed = Color(red: 197 / 255, green: 5 / 255, blue: 12 / 255)
}
