# Mobile Operations Area Scope (V1 Student-First)

## Document Control
- Area: Mobile Operations
- Owner: Wisconsin Athletics Creative Product
- Last Updated: 2026-03-25
- Status: Active
- Version: V1

## Direction
Make mobile the fastest path for student daily work while keeping staff and admin controls available without cluttering student flows.

## Why This Exists
Cheqroom mobile patterns show useful primitives but too much menu depth and too many admin actions mixed into student workflows. Gear Tracker mobile should be role-adaptive, action-first, and low-friction for checkout and reservation execution.

## Core Rules
1. Mobile is a first-class operational surface, not a reduced desktop clone.
2. Student flows prioritize `My check-outs`, `My reservations`, due/overdue handling, and scan.
3. Role-adaptive actions apply everywhere:
   - `STUDENT`: own mutations only, broad read visibility.
   - `STAFF` and `ADMIN`: global mutation access per `AREA_USERS.md`.
4. Overdue is always visually red and sorted to the top.
5. Event sync supports booking context and prefill, but V1 mobile does not require an Upcoming Events dashboard section.
6. Tap targets stay at 44px or larger.

## Mobile Navigation Contract (V1)
1. Primary destinations:
   - Dashboard
   - Items
   - Reservations
   - Check-outs
   - Scan entry point
2. Navigation can use drawer or tab patterns, but must expose scan in one tap.
3. Student view should hide admin-only create/manage affordances outside allowed ownership scope.
4. Badge counts can appear on Reservations and Check-outs for overdue or due-today urgency.

## Dashboard on Mobile
1. Keep same dashboard information architecture as desktop (`AREA_DASHBOARD.md`).
2. Prioritize:
   - Overdue banner
   - Action lanes (Check-outs, Reservations)
   - My Gear in Custody
   - Drafts
3. Do not add chart-first widgets in V1.
4. Do not add standalone Upcoming Events section in V1.

## List and Row Interaction Contract
1. Reservation and check-out rows use card-like compact summaries:
   - Time window
   - Title
   - Owner
   - Status with color dot
   - Item thumbnail strip with overflow count
2. Primary row tap opens details.
3. Secondary actions open in action sheet.
4. Search and quick filters stay pinned near top for long lists.

## Scan Experience Contract
1. Scan entry is always reachable in one tap from mobile shell.
2. Camera permission failure path must include clear fallback instructions.
3. Scan results should route directly to item or booking context when possible.
4. Failed scans must show retry without losing workflow state.

## Performance and Reliability Expectations
1. First useful mobile list content should load without heavy dashboard widgets.
2. List read models should support lightweight pagination and filter updates.
3. Offline/intermittent network states should preserve drafts and pending intent where feasible.

## Edge Cases
- Student deep-linking into admin-only mutation paths.
- Camera permission denied or revoked after first use.
- Slow network causing stale list counts versus detail state.
- Event-linked booking where event later changes or disappears.
- Mixed-location returns requiring exception handling on mobile.

## Acceptance Criteria
- [x] AC-1: Student can find and act on own due or overdue check-outs within two taps from dashboard.
- [x] AC-2: Mobile reservations and check-outs views support search, status scope, and row-to-detail navigation.
- [x] AC-3: Overdue visual treatment is red across dashboard and list contexts.
- [x] AC-4: Scan entry point is always one tap from primary mobile navigation.
- [x] AC-5: Role-based action visibility on mobile matches `AREA_USERS.md` and server authorization.
- [x] AC-6: Dashboard remains chart-light and action-first in V1.

## Dependencies
- `AREA_DASHBOARD.md`
- `AREA_CHECKOUTS.md`
- `AREA_RESERVATIONS.md`
- `AREA_ITEMS.md`
- `AREA_USERS.md`
- `AREA_EVENTS.md`

## Out of Scope (V1)
1. Native mobile app build requirements.
2. Customizable widget dashboards per user.
3. Full offline-first booking mutation queue.

## Developer Brief (No Code)
1. Define a shared mobile interaction contract for row tap, action sheet, and quick actions.
2. Ensure dashboard and list surfaces prioritize due/overdue execution over reporting widgets.
3. Keep scan entry global and fast, with explicit permission and failure handling.
4. Enforce role-adaptive visibility so student mobile stays uncluttered and policy-safe.
5. Add mobile regression coverage for ownership gating, overdue styling, and row action parity.

## Breadcrumb Roadmap

Navigation breadcrumb versioned roadmap: `tasks/breadcrumbs-roadmap.md`

All versions shipped (2026-03-25):
- **V1**: ✅ Fixed duplicate breadcrumbs, mobile text truncation
- **V2**: ✅ Entity name display on detail pages, collapsible ellipsis for deep paths
- **V3**: ✅ Sibling quick-jump dropdown, recently visited entities
- **Polish**: ✅ Loading skeleton on detail pages, parent-level sibling dropdown

## Sidebar Roadmap

Navigation shell versioned roadmap: `tasks/sidebar-roadmap.md` (revised 2026-03-25)

- **V1 (shipped)**: `SidebarMenuBadge` on Bookings (overdue) + Notifications (unread), nav groups, quick-create — closes §4 of Mobile Navigation Contract
- **V2 (next)**: Scan nav item, Settings sub-nav, due-today badge, keyboard shortcuts
- **V3 (later)**: Bottom nav badge counts via live `/api/nav-counts` polling, game-day/shift context cards

## Change Log
- 2026-05-08: **iOS item detail pass** — focused follow-up after slices 16–18 (parent link, accessories, QR chip, procurement gate, info expansion). Closes the floor-relevant Reserve gap plus today's pattern polish (edit haptic, QR feedback, a11y combined elements). (1) **"Reserve Equipment" CTA on the detail page** — full-width `.glass` button below the hero, tinted `Color.statusText(.purple)` (reservation tone). Tap opens `CreateBookingSheet` with `prefillReservation(for:)` already wired (mirrors the items-list swipe action). On save, pushes the new booking onto the parent NavigationStack via `.navigationDestination(item: $pushBooking)` (no need to manage a child path). New `AssetDetail.asAsset` helper bridges the Asset/AssetDetail type gap. Closes the "scan sticker → see → reserve" floor flow that was previously a back-out-and-restart. (2) **Edit save haptic + spinner** — `Haptics.success()` before dismiss, `Haptics.warning()` in catch; Save toolbar item swaps to inline `ProgressView` while saving. Matches the booking-detail edit fix. (3) **QR copy toast** — silent haptic-only behavior replaced with a `Toast(message: "Copied X", icon: "checkmark.circle.fill", role: .success)` so the staffer copying for kiosk re-link or Slack gets confirmation that the clipboard committed. The hero card now invokes a `onCopyQR` closure passed from the parent so the toast fires on the parent surface. (4) **Favorite haptic only fires on user action** — new `favoriteToggleCount: Int` increments only inside `toggleFavorite()`; `.sensoryFeedback(.selection, trigger:)` keys to that counter instead of `isFavorited` directly. The prior trigger fired on every load of a favorited item (annoying for repeat visitors). (5) **Accessibility:** `ActiveBookingCard`, `UpcomingReservationsCard` rows, `ParentLinkCard`, and `AccessoriesCard` rows all combine into single VO elements with explicit "Active reservation/Checked out: title, requester" / "Upcoming reservation: ..." / "Part of {tag}, {name}" / "Accessory: {tag}, {name}" labels; decorative chevrons + link icons hidden; the active-booking due-date Label gets an explicit "Overdue. Due X" / "Due X" label so VO no longer reads "exclamation triangle fill" or "clock" alongside the date. Files: `Models/AssetModels.swift`, `Views/ItemDetailView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS notifications sheet pass** — universal touchpoint (every user sees the bell badge); closes the inbox side of the notification loop after today's profile pass made push channels and quiet-hours manageable. (1) **Asset-targeted notifications now navigate to asset detail.** `checkin_item_damaged`, `checkin_item_lost`, and `low_stock` all carry `payload.assetId` but `handleTap` was falling through silently for non-booking, non-trade types. New `onSelectAsset` callback (parity with the existing `onSelectBooking` / `onSelectTrades`) plus `pendingAssetId` + `AssetRouteId` (mirrors the existing `BookingRouteId` pattern) on `HomeView`; the asset detail navigation pushes after sheet dismissal. A staffer tapping "Camera A reported damaged" lands on the asset's detail surface in one tap. (2) **Infinite scroll replaces the explicit "Load More" button.** New paginationSentinel row uses `task(id: vm.notifications.count)` to auto-fire `loadMore` when it appears; chains across page boundaries because the id changes per page. Closes the unexpected-tap UX gap. (3) **`loadMore` errors surface.** New `pageError: String?` on the view-model captures load failures; the sentinel row swaps from a `ProgressView` to an inline error + Retry button when a page fails. The prior `try?` swallowed silently. (4) **Haptic on swipe-mark-read.** New `swipeMarkHaptic` toggle wired to `.sensoryFeedback(.selection, ...)` on the list — the toolbar Mark-All-Read already had `sensoryFeedback`; per-row mark-read was silent. (5) **Accessibility:** explicit `.accessibilityLabel("Mark as read")` on both swipe buttons (leading + trailing edges) so VoiceOver no longer reads "checkmark, Mark Read" — just the action. Files: `Views/NotificationsSheet.swift`, `Views/HomeView.swift`, `Views/ItemsView.swift` (added `AssetRouteId`). Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS booking detail pass** — focused follow-up on the floor's primary action surface after multiple slice updates (live countdown, monospace ref/email, status pills, overdue banner). The 2026-04-24 broad bookings audit covered architecture; this pass closes the per-screen polish since. (1) **Ownership gate on action panel** — new `canActOnBooking` (STAFF/ADMIN OR booking requester) gates the entire `ActionsSection`. Today a STUDENT viewing someone else's booking through deep nav (Items tab → "out to {Person}" → that booking detail) saw a "Cancel Booking" button on someone else's gear; the server rejected the request, but the visual ask was alarming. Intentionally MORE permissive than `canEditBooking` because Extend is a legitimate self-help action even after OPEN ("I need it longer mid-shoot"). (2) **Action button tints** — Extend → `Color.statusText(.blue)`, Cancel → `Color.statusText(.red)` instead of raw `.blue`/`.red`. Same physical color in most cases but a single source of truth across the app. (3) **`EditBookingSheet` Save spinner + haptics** — Save toolbar item now renders an inline `ProgressView` while saving (matching the Cancel-Booking button's pattern); `Haptics.success()` fires before dismiss on the success path, `Haptics.warning()` in the catch. Closes the "did the save land?" silent-success gap. (4) **Accessibility:** explicit `accessibilityLabel` on the Extend / Cancel / overdue banner / location / date / kiosk-hint footers so VoiceOver no longer reads icon names ("clock arrow circle path", "x mark circle", "exclamation triangle fill", "map pin circle", "calendar", "barcode viewfinder") alongside the actual labels. The Cancel button's label flips to "Cancelling booking" while in-flight. Files: `Views/BookingDetailView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk success pass** — seventh and final kiosk follow-up. Smallest surface, no P0 — every fix is P1 polish on the celebration moment. (1) **"Done" button** (kioskRed capsule, "Done — return to home now") so a student who already understands "checkout complete" can leave on their own pace instead of waiting the full 5 s. Pairs with a tap-anywhere-to-dismiss `contentShape(Rectangle())` + `onTapGesture` on the outer VStack — both routes flow through a single `skip()` helper. (2) **Success haptic on appear** — `Haptics.success()` inside the `.task` modifier completes the haptic chain across every kiosk action shipped today. (3) **Symbol bounce** — `.symbolEffect(.bounce, options: .nonRepeating)` on the checkmark; reduce-motion is respected via `.nonRepeating.speed(0)`. Takes the screen from "static success text" to "celebration." (4) **Status token** — `Color.statusText(.green)` replaces raw `.green`, finishing the cross-app token discipline for the kiosk surface. (5) **`monospacedDigit()` on the countdown** so the "5s → 4s → 3s" tick doesn't jitter the layout. (6) **Accessibility:** `UIAccessibility.post(.announcement, ...)` carrying "Success. \(message)" so VoiceOver hears the result on appear; the visible elements combine into a single `.isHeader` accessibility element with label "Success: \(message)" so manual focus reads the outcome rather than three separate texts; the countdown text is `.accessibilityHidden(true)` so VO doesn't spam-read every tick; the Done button has an explicit "Done — return to home now" label. (7) **Task cancellation guard** — `if Task.isCancelled { return }` between countdown ticks so a fast Done tap doesn't race a stale tick into setting `countdown = -1` after the view dismissed. Files: `Kiosk/KioskSuccessView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk return pass** — sixth and final kiosk follow-up after activation, idle, student-hub, checkout, and pickup. Closes one **P0 ship-blocker** (exact replica of the pickup-confirm bug) plus the same shape of P1 polish + a return-specific server-truth fix. (1) **P0 — `kioskCheckinComplete` no longer swallows server errors.** Same `try? await session.data(for: req)` that broke pickup confirm broke return complete. Now routes through `perform`; 401/404/409/5xx propagate, the parent `do/catch` fires, and the user sees the real failure instead of a phantom "Return complete!" while the booking stays OPEN. (2) **Server-authoritative success counts** — server returns `{ returnedItems, totalItems, completed }` from `/api/kiosk/checkin/{id}/complete`; iOS now decodes a `KioskCheckinCompleteResult` and uses those counts in the success message. Local optimistic counts drift if a sister kiosk checked items in mid-session — server truth wins. The `completed` flag also chooses between "All N items returned. Thanks!" and "{returned} of {total} items returned." (3) **In-camera scan feedback** via the same `feedbackMessage` + `feedbackTone` parameters shipped on checkout/pickup; return adds its own `cameraTone(for:)` mapper. (4) **Scan-during-complete race guard** — "Hold on — completing return" while `isCompleting` is true. (5) **Server error surfacing** — `(error as? APIError)?.errorDescription` covers both scan and complete catch blocks, so 409 messages like "Cannot complete checkin — booking is in COMPLETED state" reach the user. (6) **Complete-failure routing** — old behavior put the error in the right-panel checklist caption (far from where the user just clicked the Complete button); now flows through `showFeedback(.error(...))` so the banner pulses next to the progress ring. Right-panel error reserved for *load* failures, with its own `wifi.exclamationmark` + retry recovery surface. (7) **Status tokens** — progress ring stroke (blue active / green done), Overdue label, item-row checkmark, "All items returned" copy, FeedbackBanner all use `Color.statusText(_:)`; `Color.kioskRed` reserved for the Complete button (intentional brand-emphasis surface for the destructive-ish completion action). (8) **Auto-scroll to last returned item** via `ScrollViewReader` + `lastReturnedId`. (9) **Returned count animates** with `contentTransition(.numericText())` + `monospacedDigit()`. (10) **Haptics** on every scan + complete event. (11) **Accessibility:** `UIAccessibility.post(.announcement, ...)` on every feedback; progress ring exposes "{returnedCount} of {totalItems} items returned"; Complete button label switches between "Complete Return, all N items" / "Return N of M items" / "Scan at least one item before returning"; `ReturnItemRow` is a combined element with the decorative checkmark hidden and a "{name}, tag {tag}, returned/pending" label; the Overdue badge gets an explicit "This checkout is overdue" label. Files: `Kiosk/KioskAPIClient.swift`, `Kiosk/KioskModels.swift`, `Kiosk/KioskReturnView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk pickup pass** — fifth kiosk follow-up after activation, idle, student-hub, and checkout. Closes one **P0 ship-blocker** plus the same shape of P1 polish shipped on checkout. (1) **P0 — `kioskPickupConfirm` no longer swallows server errors.** The prior `_ = try? await session.data(for: req)` dropped 401/404/409/5xx into the void; the parent `do/catch` never fired and the kiosk routed to `.success` even when the server returned 409 ("Scan {tagName} before confirming pickup") or 5xx. Booking stayed `PENDING_PICKUP` server-side, student walked off with gear, audit-trail orphan. Now routes through `perform` like every other API method; the actual server error surfaces to the user. (2) **In-camera scan feedback** — `KioskBarcodeCameraView` gets the `feedbackMessage` + `feedbackTone` parameters introduced in the checkout pass; pickup pipes its local `ScanFeedback` through a `cameraTone(for:)` mapper so camera scans show the same in-sheet banner over the camera feed. (3) **Scan-during-confirm race guard** — `handleScan` short-circuits with "Hold on — confirming pickup" while `isConfirming` is true. (4) **Server error surfacing** — generic "Scan failed" / "Could not confirm pickup. Please try again." now fall through to `(error as? APIError)?.errorDescription` so 409 messages like "Scan all {sku} units before confirming pickup" reach the user. (5) **Confirm-failure routing** — old behavior was a tiny red caption at the bottom of the right panel (easy to miss); now flows through `showFeedback(.error(...))` so the failure banner pulses next to the progress ring where the user is already looking. (6) **Status tokens** — progress ring, "All items confirmed" copy, item-row checkmark, Confirm button background, feedback banner colors all use `Color.statusText(_:)`; `Color.kioskRed` reserved for the in-progress ring. (7) **Auto-scroll to last confirmed item** — `ScrollViewReader` + `lastConfirmedId` keeps the freshly-checked row centered in the checklist panel; respects reduce-motion. (8) **Confirm count animates** with `contentTransition(.numericText())` + `monospacedDigit()`. (9) **Haptics** on every scan + confirm event. (10) **Detail-load error gets a real recovery surface** — `wifi.exclamationmark` icon + retry button replaces the prior tiny red text. (11) **Accessibility:** `UIAccessibility.post(.announcement, ...)` on every feedback so VO hears scan results; progress ring exposes "{N} of {Total} items confirmed"; Confirm button label switches between "Confirm Pickup, N items" / "Scan N more items before confirming" depending on state; `PickupItemRow` is a combined VO element with the decorative checkmark hidden and a "{name}, tag {tag}, confirmed/pending" label. Files: `Kiosk/KioskAPIClient.swift`, `Kiosk/KioskPickupView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk checkout pass** — fourth kiosk-surface follow-up after activation, idle, and student-hub. Closes the four floor-relevant gaps that turn small mistakes into big ones. (1) **Remove-from-cart path** — every `ItemRow` now has a trailing `xmark.circle.fill` button that drops the item from the cart with a `Haptics.warning()`. No confirmation (cost of accidental remove = one rescan; mandatory confirmation friction = high). VoiceOver gets a parallel "Remove" action via `accessibilityAction(named:)`. (2) **In-camera scan feedback** — the camera-fallback sheet was hiding the parent's `FeedbackBanner`, leaving each camera scan silent. `KioskBarcodeCameraView` now accepts a decoupled `feedbackMessage: String?` + `feedbackTone: Tone?` pair (`.success`/`.error`/`.warning`) and renders an in-sheet banner over the camera feed. The Tone enum lives on the camera view so future pickup/return camera usage can pass their own messages without coupling to checkout's enum. (3) **Scan-during-complete race** — a late HID scan during the `isCompleting` window would land in the cart but miss the assetIds payload, then get wiped on success (phantom checkout). `handleScan` now short-circuits with a "Hold on — finishing checkout" feedback during the complete API window. (4) **Server error surfacing** — generic "Scan failed" / "Checkout failed. Please try again." now fall through to `(error as? APIError)?.errorDescription` so the humanized server message ("This asset is already checked out", rate-limit copy, etc.) reaches the user. (5) **Status tokens** — scanner border, `FeedbackBanner` colors, and the green `ItemRow` checkmark now use `Color.statusText(_:)` instead of raw `.green/.red/.orange`; `Color.kioskRed` preserved for cart count + Complete button (intentional brand). (6) **Auto-scroll to latest scanned item** — `ScrollViewReader` + `onChange(of: scannedItems.last?.id)` keeps the freshest scan visible in the cart panel; respects reduce-motion. (7) **Discard alert → confirmationDialog** — matches the kiosk-deactivate pattern shipped on idle today. (8) **Cart count animates** with `contentTransition(.numericText())` + `monospacedDigit()`. (9) **Haptics on every scan event** — `.success/.warning/.error` covers HID + camera scans + remove + complete success. (10) **Accessibility:** `showFeedback` posts `UIAccessibility.post(.announcement, ...)` so VO hears scan results without manual focus navigation; Complete button label exposes "Complete Checkout, N items"; Back button label warns about the discard prompt when cart non-empty; `ItemRow` and `FeedbackBanner` are combined VO elements with decorative icons hidden. Files: `Kiosk/KioskCheckoutView.swift`, `Kiosk/KioskBarcodeCameraView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk student hub pass** — third kiosk-surface follow-up after activation + idle. Scoped to `KioskStudentHubView` (the per-name landing screen between idle → action). (1) **Top-bar avatar** uses `AsyncImage` with the existing initials disc as placeholder, matching the idle/checkout treatment shipped earlier today — picking your tile and landing on the hub no longer reads as "different page about me." (2) **Status panel deduplicated** — active checkouts were rendering twice (once as a "Return:" action button, once as a `StatusCard`); the right half now shows ONLY upcoming reservations (the one piece of context that isn't already an action button) plus a friendly empty state ("Nothing reserved this week — you're all clear"). Information hierarchy goes from "actions + duplicates" to "actions on the left, what's-coming on the right." (3) **Item summaries on action buttons** — pickup and return subtitles now render the first 1-2 item names + " · +N more" when truncated, mirroring the active-checkout pattern from the idle pass. Counts alone gave students no way to tell two pickups apart by their contents. (4) **Status tokens replace raw `.green/.blue/.orange/.red`** — pickup → `.green`, return overdue → `.orange`, return normal → `.blue`, alert tones via `Color.statusText(_:)`. Checkout stays `Color.kioskRed` (intentional brand). (5) **`StatusCard` re-keyed on `tone: StatusTone?`** instead of `isAlert: Bool` — the card grows a 3pt left-edge tone marker for glanceable alert state and routes detail-text + warning-icon colors through one source. Reservations get `.purple` (planned), overdues get `.orange`. (6) **Error state replaced with a real recovery surface** — `wifi.exclamationmark` + "Couldn't load your information" + "Try again" capsule button replaces the dead-end `Text("Could not load your information.")`. (7) **30-s refresh** — `task(id: "refresh")` mirrors the idle screen's polling so a student dawdling on the hub sees fresh state when a sister kiosk completes a return. (8) **Accessibility:** `ActionButton` and `StatusCard` combine children into single VO elements with explicit "title, subtitle" labels; decorative chevrons + status icons get `accessibilityHidden(true)`; overdue rows speak "Overdue: title, detail" so the alert state lands first; back button gets "Back to roster" label. Files: `Kiosk/KioskStudentHubView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk idle pass** — focused follow-up to the broad 2026-04-24 kiosk audit, scoped to the gear-room idle screen everyone walks up to. (1) **Avatar images on the roster grid** — `KioskUser.avatarUrl` was already fetched and decoded but discarded; now `UserTile` renders `AsyncImage(url:)` with the existing initials disc as the placeholder/failure state. Single biggest "feels like a real product" win on the floor — every Alex/Erik/Jordan now scans by photo, not by initials collision. (2) **Avatar images on active checkouts** — symmetric fix; `CheckoutRow` renders the requester's photo with the existing red-tinted overdue ring preserved as the visual signal. (3) **Deactivate confirmation** — the one-tap kill switch in the upper-right is now a destructive `confirmationDialog` ("Deactivate this kiosk? Staff will need a fresh activation code to bring this iPad back online."). Same caption-size visual footprint, no accidental brick path. (4) **Stale-data freshness stamp** — new `lastLoadedAt` state, "Updated Xs/Xm/Xh ago" caption under the kiosk name, switches to `Color.statusText(.orange)` when the last successful load is >5 min old so staff has a visual signal that the dashboard might be lying. `loadAll`'s catch block stamps `loadFailedAt` and keeps last-good values. (5) **Location subtitle** — `store.info?.locationName` now shows under the kiosk name in caption text so staff diagnosing "wrong kiosk?" reports has a quick visual confirmation. (6) **Stat tiles animate** — `contentTransition(.numericText())` + value-keyed easing so the 30-s poll deltas feel intentional; `monospacedDigit()` keeps width steady; reduce-motion respected. (7) **"+N more" cue** on truncated active-checkout item rows — when `itemCount > 2`, the row appends " · +N more" in tertiary so staff knows there's more behind the truncation. (8) **`exclamationmark.triangle.fill` switched from raw `.red` to `Color.statusText(.red)`** — token consistency. (9) **Accessibility:** `UserTile` exposes the user's full disambiguated name as the VO label (not the "EM" initials) plus a "Tap to start checkout for X" hint; `CheckoutRow`/`KioskEventRow`/`StatTile` all combine children into single VO elements; overdue rows speak "Overdue: name, items" so the most important fact lands first. (10) **TimelineView wrapper** at 30 s so the freshness stamp re-renders without depending on the data poll. Files: `Kiosk/KioskIdleView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS kiosk activation pass** — focused follow-up on the gating screen left untouched by the broad 2026-04-24 kiosk audit. (1) **Paste-from-clipboard via SwiftUI's `PasteButton(payloadType: String.self)`** — the canonical privacy-safe iOS path (no clipboard prompt, auto-hides when the clipboard doesn't carry a string). Tolerates messy pastes ("Code: 123456", "123-456") by extracting the longest run of digits and truncating to 6; auto-submits when the result is exactly 6 digits. Closes the round-trip "generate code on web → walk to iPad → retype six digits" friction. (2) **The "✓" submit key visually disables when fewer than 6 digits are entered** (`code.count != 6` → 35% opacity + `.disabled`); ⌫ likewise disables on an empty buffer. Auto-submit on the 6th digit preserved so the staffer never has to actually tap ✓. (3) **Loading overlay grounded** — replaced the floating bare `ProgressView` with a `Color.black.opacity(0.5)` scrim + centered card containing the spinner and "Activating…" copy, matching the inactivity warning's pattern shipped earlier. (4) **Error text uses `Color.statusText(.red)`** instead of raw `.red`, aligning with the cross-app `StatusTone` token system. (5) **Accessibility:** numpad keys announce actions instead of symbols (`⌫` → "Delete last digit", `✓` → "Submit code"); the digit display container exposes a single label ("Activation code, N of 6 digits entered"); on every activate failure, `surfaceError(_:)` posts `UIAccessibility.post(.announcement, ...)` so VoiceOver users hear the error immediately without manual navigation. (6) **Polish:** activation hint now says "gear.erikrole.com → Settings → Kiosk Devices" so staff isn't hunting in iOS Settings; digit-display stroke transitions on a 0.15s ease-in-out (skipped under reduce-motion); haptics on every digit/⌫/paste/success/error event. Files: `Kiosk/KioskActivationView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS scan pass** — closes the four floor-relevant gaps surfaced by `tasks/audit-scan-ios.md`. (1) **Torch toggle** added to the Scan tab's bottom overlay (parity with `QRScannerSheet`) — `AVCaptureDevice.torchMode` flip in a `.ultraThinMaterial` capsule; auto-off on `scenePhase != .active` and on tab disappear; disabled with reduced opacity when the device has no torch (sim, front cam). (2) **"Type code" manual-entry sheet for sighted users** — same path the VoiceOver `ScanManualEntryView` uses, exposed via a button on the bottom overlay AND inside the result sheet's empty/error states. New `ScanManualEntrySheet` at `[.medium]` detent, autofocuses the field, submits through the same `handleScan(_:)` path. Fixes damaged-sticker dead-ends. (3) **Server vs. unmatched-code disambiguation** — `handleScan` no longer swallows API errors with `try?`; explicit `do/catch` propagates `APIError.errorDescription` into a new `errorState` slot in `ScanResultSheet` ("Couldn't look that up" + `wifi.exclamationmark` icon + the humanized server message). Empty-state copy preserved for true zero-match scans. Both states offer "Type code instead" as recovery. (4) **Rapid-fire scan flow** — after a result lands, `isScanning` is re-enabled so a NEW code (different from the Coordinator's `lastScanned`) fires while the result sheet sits at `.medium` — the user can scan, glance, scan again without dismissing. Same-code re-fires still gated until sheet dismissal (intentional dedupe). (5) **Single-asset auto-jump** — when `SearchResults` contains exactly one asset and zero bookings/users (the canonical "scanned a sticker, got one item" case), navigate straight to `ItemDetailView` and skip the result sheet entirely. Multi-result and ambiguous codes still get the sheet. Files: `Views/ScanView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-08: **iOS profile/preferences pass** — closes the highest-traffic gap surfaced by `tasks/audit-profile-ios.md`: there was no in-app way to mute push or toggle email/push channels (web ships `/settings/notifications`; iOS shipped nothing). (1) New `Notifications` section in `ProfileView` with quiet-hours pause chips (1 h / 1 d / 1 w + Resume), email + push channel toggles, and an in-app inbox always-on footer caveat — wired to `GET/PUT /api/me/notification-preferences`. Optimistic updates revert + `Haptics.error()` on save failure; channel toggles disable while paused so the "paused" state can't be silently overridden. (2) New `NotificationPreferences` Codable on the iOS side and `APIClient.notificationPreferences()` / `updateNotificationPreferences(_:)`. (3) OS push permission state is now visible inside the app — when `UNUserNotificationCenter` reports `.denied`, a row prompts "Push disabled in iOS Settings" and deep-links to `UIApplication.openSettingsURLString`; when `.notDetermined`, an inline button re-presents `PushPrePromptView` so the user can opt in without leaving the app. Refreshes on `scenePhase == .active` so coming back from Settings reflects the new truth. (4) New `Appearance` section ports web's `/settings/appearance` theme override — System / Light / Dark, persisted via `@AppStorage("WisconsinThemeChoice")` and applied at the `WindowGroup` root via `.preferredColorScheme(_:)`. Per-device only, matching web's localStorage contract. (5) Polish: section "Dev Tools" → "Tools" (it gates the Link Sticker Wizard, a real production tool, not a debug surface); Profile sheet detents `[.large]` → `[.medium, .large]` with a drag indicator; `Open iOS Settings` deep link added to the App section so the user can reach the OS-level Wisconsin entry without leaving the app. Files: `Models/Models.swift`, `Core/APIClient.swift`, `Core/Preferences.swift` (new), `App/WisconsinApp.swift`, `Views/AppTabView.swift`, `Views/HomeView.swift`. Xcode project regenerated; entitlements (APNs + WeatherKit) restored. Build: BUILD SUCCEEDED.
- 2026-05-06: **PWA icon metadata cleanup** — Web manifest icons now use the existing 192/512 app-icon assets instead of declaring the 72px Badgers logo as an any-size manifest icon, clearing Chrome's manifest warning while preserving the Badgers logo for in-app branding.
- 2026-05-05: **Sidebar polish pass** — dashboard sidebar icon mode now treats the active nav state and user avatar as centered icon affordances instead of carrying over expanded rail/padding geometry. Trailing nav index numbers were removed, Admin separation is stronger in expanded and collapsed modes, quick-create hit targets were tightened, and shared sidebar/button transitions now use explicit transition properties with pressed scale feedback.
- 2026-05-07: **iOS item detail info expansion + procurement gate (parity slice 18)** — surfaced three more pieces of info the web detail shows but iOS hid, and closed an existing role-gating drift. (1) Models: `AssetDetail` extended with `qrCodeValue: String?`, `linkUrl: String?`, and a small `AssetMetadata` struct carrying `uwAssetTag` (other metadata fields ignored by design — only what the floor needs). (2) Hero card: a tappable QR chip below the availability snapshot — copies the sticker code to the clipboard on tap (mirrors web's small QR badge in the item header). (3) Details card: new "UW Asset Tag" row (mono, visible to all roles) so the institutional inventory ID is one tap away; new "Link" row (renders the host as a tappable Link to the manufacturer/product page, opens in Safari). **Closed an existing privacy drift**: web hides Purchase Date / Purchase Price / Link from STUDENTs under the Procurement section, but iOS was showing them to everyone. Threaded `canSeeProcurement: canEditAsset` (STAFF + ADMIN only) into `ItemDetailsCard` so the same gate applies on iOS — students no longer see procurement data. Files: `Models/AssetModels.swift`, `Views/ItemDetailView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS item detail accessories + parent link (parity slice 17)** — closes the biggest functional gap on the item detail screen. Web has dedicated `accessories` and `parentAsset` data on `/api/assets/[id]`; iOS decoded neither. (1) Added `AssetParentLink` and `AssetAccessory` Codable structs and extended `AssetDetail` with `parentAsset: AssetParentLink?` and `accessories: [AssetAccessory]?`. (2) New `ParentLinkCard` (above the hero) — shows when this asset is itself an accessory, taps through to the parent's detail so floor users can answer "what does this cable belong to?" in one tap. (3) New `AccessoriesCard` — lists every child accessory with its thumbnail, asset tag (mono), and brand/model; each row pushes that accessory's detail view, so a kit can be drilled into without returning to the list. The web's `accessories` count badge in tab labels has no equivalent (no tabs on iOS), but the count appears in the card header. Files: `Models/AssetModels.swift`, `Views/ItemDetailView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS Liquid Glass selective polish + schedule Past filter (parity slice 16)** — two small alignment moves on the most-trafficked toolbars. (1) Liquid Glass: the Bookings Mine toggle and the Schedule view-mode + My Shifts toggles all picked up `.buttonStyle(.glass)` with status-blue tint on the active state, so iOS-wide "show only mine" / mode-toggle controls feel identical and read as real selections rather than pressed buttons. Glass intentionally does NOT propagate to list rows or status pills (the documented anti-pattern). Existing glass on login / scan / floating search / booking detail actions stays as-is. (2) Schedule Past events toggle: web's list view exposes a "Past" switch that includes already-ended events; iOS API client already accepted `includePast: Bool` but no UI surfaced it. Added a `clock.arrow.circlepath` toolbar toggle (list view only, matching web), wired through `ScheduleViewModel.includePast`, force-refreshes on change. Files: `Views/BookingsView.swift`, `Views/ScheduleView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS final color sweep + notifications + schedule alignment (parity slices 13–15)** — three small alignment slices in one batch. (Slice 13) `NotificationsSheet` row icons now tint by notification *type* matching `notifIconBg` in web's notifications page — orange for due/overdue, green for shift gear up, blue for trade claimed/approved, red for trade declined/expired or damage/lost reports, purple for reservation booked/pickup-ready, gray fallback. New `notifTone` helper on `String`; uniform Wisconsin-red per-unread tint dropped (unread state still reads from the right-side dot). (Slice 14) `ScheduleView`'s "No shifts assigned to you" empty state switched from `person` (which read as a missing-user error) to `calendar`, matching schedule-empty semantics. (Slice 15) Final raw-color sweep: cleaned `LinkStickerWizard` (3) and `CreateBookingSheet` (2) leftovers. Confirmed via grep: **zero raw `Color.(red|green|orange|purple|yellow|blue)`, `systemRed/Green/Orange/Purple`, or `.foregroundStyle(.color)` literals remain anywhere in `Views/`** — the entire tree now flows through `Color.statusText/statusBackground` with `StatusTone`. Files: `Views/NotificationsSheet.swift`, `Views/ScheduleView.swift`, `Views/DevTools/LinkStickerWizard.swift`, `Views/CreateBookingSheet.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS items list subtitle alignment (parity slice 12)** — when an asset has both an asset tag and brand/model (or a custom name), the iOS row was hiding everything but the tag. Web's items table shows the brand/model as a subtitle stacked under the tag (`columns.tsx:308-340`). Ported the same pattern: new `subtitleWhenTagIsPrimary` computed prop on `AssetRow` returns the custom name (when set) or `brand model`, suppressed when it's just a casing/whitespace duplicate of the tag — matches the web's `rawSubtitle.toLowerCase() !== item.assetTag.toLowerCase()` guard. Files: `Views/ItemsView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS empty-state alignment + item availability snapshot (parity slice 11)** — two passes. (1) Empty-state alignment: cataloged web's `EmptyState` icon vocabulary (`search/calendar/box/clipboard/bell/users/folder/chart/wifi-off/check`) and corrected three iOS drifts: `BookingsView` empty `tray` → `archivebox` (matches `box`), `NotificationsSheet` empty `bell.slash` → `bell` (matches `bell`), `AssignStudentSheet` empty `person.3` → `person.2` (matches `users`). Replaced 8 generic "Error" titles with specific "Couldn't load X" copy across `HomeView`, `BookingsView`, `UserDetailView`, `ItemsView`, `ItemDetailView`, `BookingDetailView`, `NotificationsSheet`, `TradeBoardSheet`. (2) Availability snapshot on item detail: new `availabilitySnapshot(for:)` helper renders a one-line answer to "can I grab this right now?" inline in the hero card — `[Requester] · due back in 3 hours 12 minutes` for active bookings (uses the slice-9 countdown), `Available — next reserved [date]` when free with an upcoming res, `Available` when fully open, `Out for maintenance` / `Retired from service` for those statuses. Also cleaned `ActiveBookingCard`'s lingering `.purple/.blue/.red` literals to use status tokens via `StatusTone`. Files: `Views/HomeView.swift`, `BookingsView.swift`, `UserDetailView.swift`, `ItemsView.swift`, `ItemDetailView.swift`, `BookingDetailView.swift`, `NotificationsSheet.swift`, `Schedule/TradeBoardSheet.swift`, `Schedule/AssignStudentSheet.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS bulk thumbnails + Mine filter (parity slice 10)** — two floor-relevant alignment moves. (1) `BookingDetailView.BulkSection` now renders an `AsyncImage` thumbnail per consumable, mirroring web's `BookingEquipmentTab.tsx:327` pattern (image when `bulkSku.imageUrl` is set, neutral `shippingbox` placeholder otherwise). `BulkSku` model gained `imageUrl: String?` (already returned by `/api/bookings/[id]`). (2) New "Mine" toggle in the Bookings tab toolbar (top-leading) — single-tap to filter the list to bookings the current user requested, accomplished by passing `requester_id` to the existing API. `APIClient.reservations` and `.checkouts` gained an optional `requesterId:` parameter; `BookingsViewModel` gained `mineOnly` + `currentUserId` and skips the `GearStore` cache seed/read when the filter is on (cache holds everyone's bookings). Empty-state copy changes when Mine is on ("Nothing checked out or reserved by you."). Files: `Models/Models.swift`, `Core/APIClient.swift`, `Views/BookingDetailView.swift`, `Views/BookingsView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS booking detail live countdown (parity slice 9)** — ports the web's countdown badge to the iOS booking detail screen for active checkouts only. New `UrgencyLevel` enum + `Date.bookingUrgency(...)` + `Date.countdownLabel(...)` helpers in `DateFormats.swift` mirror `getUrgency` and `formatCountdown` in `src/lib/format.ts` exactly (≤10% remaining = critical, ≤25% = warning, otherwise normal; "DUE BACK IN N hours M minutes" vocabulary). `BookingDetailView.HeaderSection` now wraps a `StatusPill` in a 30-second `TimelineView(.periodic)` so the badge ticks live without manual Timer plumbing. Tone follows urgency: red for overdue/critical, orange for warning, blue for normal. Only renders when status is `.open` (active checkout) — booked/pending/cancelled bookings still show their static status badge. **Did NOT** port the Users-page roster summary band, web's recharts overdue-hours chart, or other power-user info — flagged by user as "These are power user for web," and codified in `feedback_ios_vs_web_role.md` ("Summary / stats bands are power-user info — keep them on web only"). Files: `Core/DateFormats.swift`, `Views/BookingDetailView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS app-wide color token sweep (parity slice 8)** — closes the loop on the consistency rule by migrating every remaining non-DevTools view to `Color.statusText/statusBackground`. (1) `BannerView` — both `.warning` and `.error` gradients now use the status tokens, propagating to every banner mount across the app (offline banner, refresh-failure pill, session warnings). (2) `LoginView` — error text. (3) `CreateBookingSheet` — error text + scheduling-conflict orange. (4) `EventDetailSheet` — replaced inline "Pending" pill with `StatusPill(.orange)`; Approve/Decline button tints use status tokens. (5) `ScheduleView` — Home/Away dot uses green/orange status tokens. (6) `Schedule/AddShiftSheet`, `PostTradeSheet` — error text. (7) `GlobalSearchSheet` — destructive "Clear Recents" button. (8) `SearchResultRow` — checkout/reservation icon tinting now uses `.blue` / `.purple` from the status taxonomy. Confirmed via grep: zero raw `Color.(red|green|orange|purple|yellow)`, `systemRed/Green/Orange/Purple`, or `.foregroundStyle(.color)` literals remain across the entire `Views/` tree (DevTools intentionally skipped — staff-only surface, low traffic). Files: 9 modified. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS detail-screen alignment (parity slice 7)** — extends the token system + typography rules to the highest-traffic detail screens. `BookingDetailView`: refactored `allocationLabel` to return `(label, StatusTone)` and replaced the inline pill with `StatusPill` (matches web's status-styles.ts mapping for active/returned/draft); migrated three `.foregroundStyle(.red)` callsites + one `Color(.systemRed)` overdue banner to `Color.statusText(.red)`; ref number, requester email, and asset tag now render in `.system(_, design: .monospaced)` matching web's `font-mono`. `ItemDetailView`: error text → `Color.statusText(.red)`; "Upcoming Reservations" calendar icon → `Color.statusText(.purple)` (purple = reservations per web's status taxonomy). `ItemsView` row: when an asset has an assetTag, the primary line renders in SF Mono (matches web's `--font-mono` treatment for IDs); brand+model fallback stays in SF Pro. Confirmed via grep: zero raw `Color.red/.green/.orange/.purple` or `systemRed/Green/Orange` literals remain across `BookingDetailView`, `ItemDetailView`, and `ItemsView`. Files: `Views/BookingDetailView.swift`, `Views/ItemDetailView.swift`, `Views/ItemsView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS quick-extend chips on booking detail (parity slice 6)** — partial port of the user's "extend / return / transfer" floor-action ask. After auditing web: extend already shipped on iOS, return is kiosk-only by project rule (not portable to iOS), transfer doesn't exist on web either — so this slice focuses on bringing extend to web parity. `ExtendBookingSheet` now has +1 day / +3 days / +1 week quick chips above the date picker, mirroring `BookingDetailPage.tsx:404-418`. Chips offset from the picker's current value if the user already nudged it, otherwise from the booking's current end (matches `handleQuickExtend`). Haptics added: tap on preset, success on extend, warning on error. Color/typography polish: error text uses `Color.statusText(.red)`, current-end timestamp uses `.monospacedDigit()`. Files: `Views/ExtendBookingSheet.swift`. Build: BUILD SUCCEEDED. Pending user clarification on what "transfer" means — could mean reassign-requester (admin/web) or split-equipment-off-booking (doesn't exist anywhere yet).
- 2026-05-07: **iOS dashboard color cleanup (parity slice 5)** — finishes the status-token migration across the home dashboard. Migrated `OverdueBanner`, `RefreshFailurePill`, `AllClearEmptyState`, `BookingSummaryRow` (bar color, requester avatar, overdue label), `DashboardShiftRow` ("has gear" check), `EventSummaryRow` (home/away bar, coverage indicator, home/away pill), `FlaggedItemsBanner`, and `LostBulkUnitsBanner` from raw `Color.red/.orange/.green/.purple` literals to `Color.statusText(_:)` and `Color.statusBackground(_:)`. Banner strokes use `Color.statusText(tone).opacity(0.2)` so the visible border remains the saturated status color while the fill stays subtle (matches web's Alert pattern). One token used in `BookingsView` row overdue icon as well. Confirmed via grep: zero raw `Color.red/.green/.orange/.purple` literals remain in either file. Files: `Views/HomeView.swift`, `Views/BookingsView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS consistency follow-ups (parity slice 4)** — extended slice 3's token system to two more surfaces and made one typography decision durable. (1) `BookingsView.StatusBadge(status:kind:)` now renders via `StatusPill` internally — preserves the existing booking-status API while removing all local `.gray/.purple/.blue/.orange` literals. (2) Dashboard `StatStrip` cells now carry per-cell `StatusTone?` instead of a binary `isAlert` flag: Overdue → red, Due Today → orange, Checked Out → blue, Reserved → purple (matches web's status taxonomy); zero values stay neutral so the strip doesn't shout when nothing needs action. Numeric values pick up `.monospacedDigit()` for steady width as counts change. (3) Typography decision captured in `feedback_ios_little_brother.md`: keep SF Pro on iOS — mirror web's `--font-heading` / `--font-mono` *roles* via SF Pro weights and `.monospaced()`, not by porting the typeface. Email rows in `UsersView` and `UserDetailView` now use `.system(.{caption,subheadline}, design: .monospaced)` — matches web's `font-mono` for emails. Files: `Views/BookingsView.swift`, `Views/HomeView.swift`, `Views/UsersView.swift`, `Views/UserDetailView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS visual consistency with web (parity slice 3)** — establishes iOS as the "little brother" of web with shared design tokens. New `StatusTone` enum + `Color.statusText(_:)` / `Color.statusBackground(_:)` helpers in `Brand.swift` mirror web's `--{tone}-text` / `--{tone}-bg` token pairs (red/blue/green/orange/purple/gray) with dark-mode adaptive values. New `StatusPill` shared component (`Views/Components/StatusPill.swift`) matches web's `<Badge variant="...">` styling — paired bg/text colors, capitalized labels, optional emphasized typography. `StatusPill.role(_:)` matches web's `RoleBadge` exactly: ADMIN=purple, STAFF=blue, **STUDENT=gray** (was incorrectly green in slice 1 — fixed). Migrated: `UsersView` Inactive pill + role pill; `UserDetailView` profile-header avatar + role chip (was hardcoded purple for all roles); `OverdueReportView` count pill + red text. Existing `StatusBadge(status:kind:)` in `BookingsView.swift` left untouched for now — uses different API (booking status); future work to consolidate. Files: `Core/Brand.swift`, `Views/Components/StatusPill.swift` (new), `Views/UsersView.swift`, `Views/UserDetailView.swift`, `Views/OverdueReportView.swift`. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS Overdue report (parity slice 2)** — replaces the placeholder Profile → "Overdue" destination with a real leaderboard for STAFF/ADMIN. New `OverdueReportView.swift` shows total overdue checkouts, count of people with overdue gear, and an expandable per-person list sorted by total overdue time. Tap a booking row → existing `BookingDetailView`. New `OverdueReport`/`OverdueLeaderboardEntry`/`OverdueBookingSummary` models in `Models.swift`. New `APIClient.overdueReport()` calling `/api/reports/overdue` (no envelope, decoded directly). `ProfileDestinationView` gates: STAFF/ADMIN see the report, STUDENT keeps the existing "Open the Bookings tab" placeholder (server enforces report:view = ADMIN/STAFF anyway). Web equivalent (chart + CSV export) intentionally NOT ported — power-user features stay on web. Files: `ios/Wisconsin/Views/OverdueReportView.swift` (new), `Models/Models.swift`, `Core/APIClient.swift`, `Views/AppTabView.swift`. Xcode project regenerated; entitlements restored. Build: BUILD SUCCEEDED.
- 2026-05-07: **iOS Users tab (parity slice 1)** — first parity step toward web parity. New `UsersView.swift` lists users with debounced search, role filter (Admin/Staff/Student), show-inactive toggle, and infinite-scroll pagination at 50/page. Tap row pushes existing `UserDetailView` via new `UserRouteId` wrapper (avoids collision with UserDetailView's String-typed booking-id destination). `AppUser` model extended with `primaryArea`, `title`, `active`, `gradYear`, `studentYearOverride` (matches `/api/users` response). `APIClient.users()` now accepts `role`, `includeInactive`, `offset`, default limit raised to 50. New 6th tab "Users" (person.2) added to `AppTabView`. Files: `ios/Wisconsin/Views/UsersView.swift` (new), `Models/Models.swift`, `Core/APIClient.swift`, `Views/AppTabView.swift`. Xcode project regenerated; entitlements (APNs + WeatherKit) restored. Build: BUILD SUCCEEDED on iOS Simulator.
- 2026-04-25: **iOS schedule authoring** — closes the role-gated authoring gap on the Schedule surface. STAFF/ADMIN can now (1) assign students to open shift slots from the event's sport roster (or any user via search if the event has no sportCode) via `AssignStudentSheet` — fetched from `/api/sport-configs/[code]/roster`; (2) add new shifts to an event via `AddShiftSheet` (area + worker type + optional time override) — POST `/api/shift-groups/[id]/shifts`; (3) remove an assignment via context-menu on the assigned name with destructive confirm — DELETE `/api/shift-assignments/[id]`. STUDENTs can request open ST slots (premier or otherwise) — confirm dialog explains "Staff will review your request" for premier events — POST `/api/shift-assignments/request`. REQUESTED-state assignments now render a "Pending" pill so staff can see who's waiting for approval. New iOS API client methods: `sportRoster`, `assignShift`, `unassignShift`, `requestShift`, `addShift`. New views: `Schedule/AssignStudentSheet.swift`, `Schedule/AddShiftSheet.swift`. Models: added `RosterEntry` / `RosterUser`. Xcode project regenerated.
- 2026-04-25: **iOS cross-cutting polish pass** — Profile collapsed from 6th tab to a top-bar avatar on Home (5-tab nav now within HIG); soft push pre-prompt (`PushPrePromptView`) shown after first login instead of cold OS alert (improves opt-in, follows HIG); centralized 401 handling — APIClient posts `.sessionDidExpire`, SessionStore clears `currentUser` so the user is auto-routed to LoginView (per-VM "Session expired" strings dropped from Schedule + EventDetail); unified `BannerView` component replaces the three separate banner styles in RootView / AppTabView; `Haptics` enum + `Date.gearShort/Long/Time/freshnessLabel` extensions + `Color.brandPrimary/brandSurface/brandSurfaceDim` tokens give one source of truth; "Updated Xm ago" subtitle now appears on Bookings / Items / Schedule lists; Sign Out gets a destructive `confirmationDialog`; ProfileView gains `NavigationLink`s on the stat strip + a "Manage account on web" Link to gear.erikrole.com; `appState.refresh()` (full counts) replaces `refreshUnread()` on `scenePhase == .active` so badges stay current regardless of which tab the user opens first; LoginView raw RGB swapped for `Color.brandPrimary/Surface/SurfaceDim`. Files: 13 modified, 5 new (Brand, DateFormats, Haptics, BannerView, PushPrePromptView). Xcode project regenerated. Build verification pending.
- 2026-04-24: **iOS schedule audit fixes + niceties** — closes 1 P0 + 7 P1 from `tasks/audit-schedule-ios.md`. (1) Refresh failure no longer blanks an already-populated screen — events branch wins, refresh error becomes a non-blocking banner with Retry; (2) `ScheduleViewModel.lastLoadedAt` + `isStale` (5 min) and a `scenePhase == .active` hook make tab/scene re-entry refresh automatically; (3) calendar `displayedMonth` ↔ `selectedDate` now move together via shared `changeMonth(by:)` + `goToToday()`; (4) "My Shifts" empty state shows a `ContentUnavailableView` instead of blanking; (5) PostTradeSheet Cancel disables while posting + discard-changes confirm; (6) trade-board "My Active Posts" swipe routes through a destructive `confirmationDialog` matching the Claim pattern; (7) EventRow shadow uses `Color.primary` + `Color(.separator)` border for dark-mode lift; (8) `MyShiftStatus` enum replaces the string compare in `eligibleShifts`. Niceties: "Today" pill in calendar header when off-month; horizontal swipe to change month; calendar dot legend (My Shift / Home / Away); success toasts after post + claim ("Posted X shift", "You picked up X on Y"); event sheet uses `[.medium, .large]` detents; "Updated Xm ago" subtitle in List mode; trade-board toolbar button now shows `(N)` count pill instead of a tiny corner badge; CALL block hidden in EventRow when call time equals event start. Files: `ios/Wisconsin/Views/ScheduleView.swift`, `Schedule/PostTradeSheet.swift`, `Schedule/TradeBoardSheet.swift`. Build verification pending in Xcode.
- 2026-04-24: **iOS login audit P2 polish** — added "Need an account?" Link to web `/register` (registration stays web-only, gated by AllowedEmail per D-2026-04-03); `SessionStore.clearError()` now wipes the stale 401 the instant the user starts typing again; logout body documents the deliberate `try?` swallow. Face ID re-auth + iPad header polish deferred until those become priorities.
- 2026-04-24: **iOS login audit fixes** — closes 4 P1 from `tasks/audit-login-ios.md`. (1) "Forgot password?" Link added, opens `gear.erikrole.com/forgot-password`; (2) email is trimmed + lowercased before submit, fixing autocomplete-trailing-whitespace 401s; (3) Return-key submit dismisses keyboard via shared `submit()` helper so the loading button is visible and double-submits are blocked; (4) input shadows replaced with `Color(.separator)` borders for dark-mode parity. `textContentType(.username)` / `.password` added so iCloud Keychain autofill works. Files: `ios/Wisconsin/Views/LoginView.swift`. Build verification pending in Xcode.
- 2026-04-24: **iOS items audit P2 polish** — purchase price uses `Locale.current.currency`, Reserve completion navigates to the new booking via `BookingRouteId` on the items NavigationStack. (Initial pass also hid requester names from STUDENTs but that was reverted by user decision: small team, ownership visibility wins.) AC-8 admin actions menu logged as GAP-36 for staff-mobile parity.
- 2026-04-24: **iOS items audit fixes** — closes 1 P0 + 7 P1 from `tasks/audit-items-ios.md`. (1) `ItemsViewModel.load()` now uses the same `loadTask` cancel-on-reset pattern as bookings so filter/favorites toggles never strand stale rows; (2) Reserve action from item context menu now prefills the booking sheet with the asset preselected, the asset's home location, and a sensible title via new `CreateBookingViewModel.prefillReservation(for:)`; (3) detail favorite toggle wraps `do/catch` and reverts UI + alerts on failure (no more silent lie); (4) detail edit pencil gated on STAFF/ADMIN per AC-5/AC-7; (5) `EditAssetSheet` gets discard-changes confirm + `interactiveDismissDisabled` while saving; (6) pagination errors render inline Retry; (7) `AssetThumbnail` border switched to `Color(.separator)` for dark mode; (8) toolbar trailing buttons get 44pt `frame` + 12pt spacing. Files: `ios/Wisconsin/Views/ItemsView.swift`, `ItemDetailView.swift`, `CreateBookingSheet.swift`. Build verification pending in Xcode.
- 2026-04-24: **iOS bookings audit fixes** — closes 1 P0 + 6 P1 + 3 P2 from `tasks/audit-bookings-ios.md`. P0/P1: (1) Tab switch cancels in-flight load via stored `Task` handle; (2) AC-5 hardening — toolbar `+`, requester picker, and edit pencil all role/ownership-gated; (3) pagination errors render inline Retry instead of forever-spinner; (4) Create + Edit sheets show "Discard changes?" + disable interactive dismiss while submitting; (5) FormCard / AssetPickerRow switched off hardcoded black for dark-mode parity. P2: (6) `nextCleanHour` rewritten with explicit `addingHours:` semantics; (7) toolbar Picker uses `maxWidth: 260` + `fixedSize` so it shrinks instead of clipping on narrow devices; (8) trailing pagination row uses `.task(id:)` instead of `.onAppear`. Two parity items deferred as GAP-34 (status filters/sort) and GAP-35 (conflict badges). Files: `ios/Wisconsin/Views/BookingsView.swift`, `BookingDetailView.swift`, `CreateBookingSheet.swift`. Build verification pending in Xcode.
- 2026-03-02: Initial mobile operations area scope created from Cheqroom mobile analysis and Gear Tracker role model.
- 2026-03-15: Student Mobile Hardening V1 shipped — STUDENT added to checkout.scan permission with server-side ownership gating, sidebar hides Users/Kits/Settings for STUDENT, team activity hidden on mobile for STUDENT, ownership border accent on My Gear rows.
- 2026-03-22: iPhone polish pass — (1) Fixed iOS input zoom: Input/Textarea/SelectTrigger use `text-base md:text-sm` (16px mobile, 13px desktop). (2) Global `-webkit-tap-highlight-color: transparent` on all interactive elements. (3) `overscroll-behavior-y: none` on body. (4) Booking detail header stacks title above action buttons on mobile (`flex-col sm:flex-row`). (5) Equipment card header stacks title above return buttons on mobile. (6) Row action menus always visible on mobile (hover-reveal only on sm+).
- 2026-03-23: Scan page hardening (5-pass) — Skeleton loading states, shadcn Alert for errors, optimistic checklist updates, auto-clear feedback, processingRef guards on all scan handlers, network drop recovery via try/catch/finally.
- 2026-03-24: Sidebar V1 badges shipped — closes §4 of Mobile Navigation Contract. Overdue badge on Checkouts, unread badge on Notifications nav item (both from AppShell parallel fetch). Semantic nav groups (Operations / Admin) with STUDENT seeing no Admin group. Kits moved to Admin group with "Soon" badge. GAP-10 closed. Hardening (5-pass): dead old-sidebar CSS removed (107 lines), AbortController cleanup on badge fetch, isLoggingOut guard on logout button, logout network-failure recovery, profile nav item removed (redundant with header avatar), group separators, count-aware collapsed tooltips. Stress test: fixed overdue badge always showing 0 (wrong response path dashJson.stats vs dashJson.data.stats); fixed system-wide overdue being shown to STUDENT (added user-scoped myOverdueCount to dashboard response).
- 2026-03-25: Doc sync — standardized ACs to checkbox format, all 6 checked.
- 2026-04-09: **Sidebar redesign** (commit ed6e2eb) — UW Athletics brand identity. Gotham brand font applied to nav labels for program-specific identity. Trailing item index numbers (01–09) in Geist Mono; turn Wisconsin Red on active. User role (ADMIN/STAFF/STUDENT) displayed in mono under name in user card. Section separator replaced with centered `─── ADMIN ───` rule. UW Athletics subtitle in mono above Gear Tracker wordmark. Active background tint reduced to 10% so left-border red does the signaling. File modified: `Sidebar.tsx` (262→165 lines net due to visual restructuring).
- 2026-04-09: **Booking list migrated to shadcn Table** (commit d1bbc8d) — Unified status visual system. `getStatusVisual` now returns Tailwind classes (`rowClass`/`titleClass`) instead of legacy CSS class names (`status-overdue`, etc.). `BookingRow` uses `TableRow`/`TableCell` instead of native tr/td. Dead CSS removed: `.hide-mobile`, `.row-link`, `.status-*` rules (37 lines). Cancelled gets `line-through`, completed gets `opacity-60`, overdue gets `text-destructive` — now consistent on mobile and desktop (previously mobile had no status styling). Files modified: `BookingListPage.tsx`, `BookingRow.tsx` (+66 lines, +rules → Tailwind), `SortHeader.tsx`, `BookingCard.tsx` (status styling). Globals.css cleaned (37 lines removed).
- 2026-04-09: **Linear/Notion design refresh** (commit 425a698) — Phases 0–3 of design refresh. Phase 0 (globals.css): WCAG AA contrast fixes on text-secondary/text-muted; deduplicate keyframes; remove legacy radius vars; touch target scoping; add login-bg utility. Phase 1 (components): button disabled:grayscale, tooltip delayDuration 200ms, skeleton shimmer in both themes, alert/alert-dialog semantic variants, select separator bg-border, card remove hover translate, motion reduce durations. Phase 2 (critical pages): calendar-sources, labels, database, allowed-emails, venue-mappings, categories, bulk-inventory → shadcn Table; remove inline styles; standardize headings. Phase 3 (medium-priority): import Alert, sports/escalation/notifications wrap in FadeUp, text-secondary → text-muted-foreground sweeps across search/badge/scan. Overall: 30+ files touched, legacy CSS marked DEPRECATED.
