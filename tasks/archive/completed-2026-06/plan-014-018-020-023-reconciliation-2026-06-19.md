# Plan 014, 018, and iOS Picker Reconciliation - 2026-06-19

Moved from `tasks/todo.md` so the active queue only advertises current work. These sections were fully checked off and retained here for review and proof history.

---

## Active: Plan 014 kiosk checkout completion friendly conflict reconciliation (2026-06-19)

- [x] Confirm live route drift against Plan 014: transactional availability validation is already present.
- [x] Add the remaining last-second allocation constraint fallback.
- [x] Add focused checkout-complete regression coverage for kiosk default location and allocation race mapping.
- [x] Sync Plan 014 status and kiosk docs.
- [x] Run focused and repo-level verification.

### Review
- 2026-06-19: Plan 014 is reconciled on main. Kiosk checkout completion already revalidated availability inside the transaction; this pass added the residual `asset_allocations_no_overlap`/allocation constraint fallback so races become friendly 409s, plus focused route tests for kiosk default location and race mapping.
- Verification: `npx vitest run tests/kiosk-checkout-complete-bulk-units.test.ts`; `npx vitest run tests/kiosk-checkin-routes.test.ts`; `npx tsc --noEmit`; `npm run lint`; `npm run test`; `npm run verify:docs`; `git diff --check`.

---

## Active: Plan 018 resumed kiosk pickup progress reconciliation (2026-06-19)

- [x] Confirm live drift against Plan 018 and preserve newer reservation-pickup behavior.
- [x] Fill already-picked numbered-bulk slots for `CHECKOUT/PENDING_PICKUP` detail responses.
- [x] Seed native pickup checklist state from server progress on detail load.
- [x] Add focused route regressions for resumed serialized and numbered-bulk pickup progress.
- [x] Sync Plan 018 status and kiosk docs.
- [x] Run repo-level verification.

### Review
- 2026-06-19: Plan 018 is reconciled on main. Resumed pending-pickup detail now returns already-picked numbered battery slots as complete, and the native pickup screen seeds confirmed rows from server detail progress so already-scanned items do not block final confirmation.
- Verification: `npx vitest run tests/kiosk-bulk-detail-routes.test.ts`; `npx tsc --noEmit`; `npm run drift:ios`; `npm run test`; `npm run lint`; `npm run verify:docs`; `git diff --check`; XcodeBuildMCP `build_sim` for `Wisconsin` on iPhone 17 iOS 26.5.

---

## Active: Plan 020 iOS booking picker display-aligned sort (2026-06-19)

- [x] Add `/api/assets?sort=name` with brand, model, then asset-tag ordering.
- [x] Let the iOS API client send an optional `sort` query parameter.
- [x] Make native reservation creation request `sort: "name"` for the Equipment picker.
- [x] Add source-contract coverage for server, client, and picker wiring.
- [x] Sync Plan 020 status and docs.
- [x] Run repo-level verification.

### Review
- 2026-06-19: Plan 020 is reconciled on main. `/api/assets?sort=name` now sorts serialized assets by brand, model, then asset tag, and native reservation creation requests that sort for its Equipment picker so the row order matches the displayed product name.
- Verification: `npx vitest run tests/ios-create-booking-picker-parity.test.ts`; `npx tsc --noEmit`; `npm run drift:ios`; `npm run test`; `npm run lint`; `npm run verify:docs`; `git diff --check`; XcodeBuildMCP `build_sim` for `Wisconsin` on iPhone 17 iOS 26.5.

---

## Active: Plan 021 iOS booking picker bulk photos (2026-06-19)

- [x] Expose `BulkSku.imageUrl` from `/api/form-options`.
- [x] Decode `imageUrl` on native `FormBulkSku`.
- [x] Render SKU photos for bulk picker rows and selected bulk rows with the existing box fallback.
- [x] Add endpoint and iOS source-contract coverage.
- [x] Sync Plan 021 status and docs.
- [x] Run focused and repo-level verification.

### Review
- 2026-06-19: Plan 021 is reconciled on main. Native reservation creation now shows SKU photos for batteries and counted items in the Equipment picker and selected list, while SKUs without images keep the previous shipping-box fallback.
- Verification: `npx vitest run tests/form-options-bulk-counts.test.ts tests/ios-create-booking-picker-parity.test.ts`; `npx tsc --noEmit`; `npm run drift:ios`; `npm run test`; `npm run lint`; `npm run verify:docs`; `git diff --check`; XcodeBuildMCP `build_sim` for `Wisconsin` on iPhone 17 iOS 26.5.

---

## Active: Plan 022 iOS picker category grouping design spike (2026-06-19)

- [x] Confirm Plan 020 dependency is already landed.
- [x] Inspect native picker pagination and `/api/assets` sorting/filtering contract.
- [x] Gather live inventory-size evidence for one-location picker viability.
- [x] Write the design decision doc.
- [x] Add the DECISIONS pointer and plan status.
- [x] Verify docs and whitespace.

### Review
- 2026-06-19: Plan 022 is reconciled on main as a design-only slice. The accepted direction is a bounded full fetch for the selected location, grouped client-side by category, using existing `/api/assets` fields and Plan 020's `sort=name`. The future implementation should extend native `APIClient.assets` with `location_id`, request up to 300 available serialized assets, and show search recovery if the cap is exceeded.
- Verification: `npm run verify:docs`; `git diff --check`.

---

## Active: Plan 023 iOS picker category grouping implementation (2026-06-19)

- [x] Add `location_id` query support to native `APIClient.assets`.
- [x] Fetch selected-location available serialized assets with `sort=name`, `limit=300`, and `offset=0`.
- [x] Render native Equipment picker serialized rows in category sections.
- [x] Add capped-result search recovery copy.
- [x] Add source-contract coverage.
- [x] Sync plan and area docs.
- [x] Run repo-level verification.

### Review
- 2026-06-19: Native reservation creation now groups available serialized equipment by category for the selected pickup location and replaces the previous flat 30-row append path for this picker.
- Verification: `npx vitest run tests/ios-create-booking-picker-parity.test.ts`; `npx tsc --noEmit`; `npm run drift:ios`; `npm run test`; `npm run lint`; `npm run verify:docs`; `git diff --check`; XcodeBuildMCP `build_sim` for `Wisconsin` on iPhone 17 iOS 26.5.
